var person = {
  name: 'Andrew'
};

person.age = 25;

debugger; //break point, começa debugar aqui, se tirar, debuga desde o começo, mas o repl nao funciona

person.name = 'Mike';

console.log(person);
